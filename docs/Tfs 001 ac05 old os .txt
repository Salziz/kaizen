Kaizen TFS-001 — AC05 evidence (part 2 of 2)
Criterion: The app opens and stays stable on a real device whose OS is at
least two major versions behind the latest release.

Device: Android Emulator, API 31 (Android 12)
Context: current latest is Android 16 (API 36); the stated two-major-
version floor computes to API 34 (Android 14). API 31 exceeds that
requirement with margin — this is the same emulator image used to
determine and verify the project's `minSdk` floor (see
android/app/build.gradle.kts).

Procedure:
1. Launched an API 31 (Android 12) AVD.
2. Installed the release APK (signed with the project's production
   keystore).
3. Launched Kaizen from a cold start.
4. Confirmed the app opened, rendered its screen, and remained stable
   through basic navigation and a background/foreground cycle.

Result: PASS. App installed and ran cleanly on API 31, comfortably
clearing the two-major-version-behind requirement (API 34).